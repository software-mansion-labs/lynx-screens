package com.lynxscreens.screens.header.toolbar

/**
 * Defaults for toolbar menu item fields.
 *
 * Must stay in sync with the JS types. The native side is the source
 * of truth for defaults of nested-object fields.
 */
internal object StackHeaderToolbarMenuItemDefaults {
    const val TITLE: String = ""
    const val HIDDEN: Boolean = false
}
