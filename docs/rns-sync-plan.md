# RNS → lynx-screens sync plan (Stack v5)

Working checklist for backporting `react-native-screens` Stack v5 commits into `lynx-screens`.

- **Synced up to:** [`63b3baab`](https://github.com/software-mansion/react-native-screens/commit/63b3baab65a1fd36da04ae426f98ad460217e1e0)
- **Target:** [`c0305806`](https://github.com/software-mansion/react-native-screens/commit/c030580604fc89d30ea13b1e56a3b9f398c4fcce) (RNS `main` as of 2026-08-18) — 418 commits in range
- **Method:** walk the range oldest → newest and backport every commit that touches Stack v5, **always one commit at a time, strictly in RNS order** — no batching, no squashing. One RNS commit per PR, with `Related commit from RNScreens: <link>` in the PR description.
- **Examples:** when the RNS commit adds an example/test screen, port the example into `LynxExample/src/examples` and set it active in `LynxExample/src/App.tsx` in the same commit, so it can be tested right after checkout. Never port `scenario.md` files or scenario descriptions — the example only.
- **Statuses:** `[ ]` pending · `[x]` backported · `[-]` reviewed, not applicable (note why)

Out of the 418 commits, ~55 are in the backport queue below. The rest is: Tabs/Split/ScrollViewMarker (~70), FormSheet v5 (~45, out of scope — see below), Stack v4 / legacy cleanups, e2e tests, examples, docs and CI.

Sizes are rough (files touched in relevant paths): **S** ≤3, **M** ≤12, **L** >12.

## Backport queue (chronological)

Lanes: `core` = existing lynx-screens code (Host/Screen/model), `hdr-and` / `hdr-ios` = header epic (new functional area for lynx-screens), `example` = `StackContainer` in LynxExample.

- [-] `17a668299` S core-ios — fix(iOS, StackV5): Fix default props type ([#3800](https://github.com/software-mansion/react-native-screens/commit/17a668299)) — N/A: fixes a wrong Fabric default-props struct in `resetProps`; lynx-screens sets plain ivar defaults via `LYNX_PROP_SETTER`, no props structs exist
- [x] `a68e00538` M core-js — chore: make all exports from v5 component modules explicit ([#3808](https://github.com/software-mansion/react-native-screens/commit/a68e00538)) — only the stack type exports apply; lynx-screens had no default exports
- [x] `a821ff0af` L hdr-and — feat(Android, Stack v5): add basic support for header ([#3753](https://github.com/software-mansion/react-native-screens/commit/a821ff0af)) — Fabric state/C++ shadow node replaced with Lynx `StackScreenShadowNode` (CustomMeasureFunc); header height NOT forwarded as content offset (native wrapper already offset — see comment in `StackScreenCoordinatorLayout`)
- [x] `33feebbb3` L core-ios — feat(iOS, Stack v5): Align Stack implementation with RFC 753 ([#3774](https://github.com/software-mansion/react-native-screens/commit/33feebbb3)) — Swift controllers replaced with ObjC; ops queue + coordinator ported; RCTMountingTransactionObserving emulated with the existing dispatch_async transaction-finish trick
- [x] `29b2a2b2a` M core-js — chore(types): enable `exactOptionalPropertyTypes` support ([#3719](https://github.com/software-mansion/react-native-screens/commit/29b2a2b2a)) — flag enabled in root tsconfig; `| undefined` added in StackScreen types and lynx-elements IntrinsicElements
- [x] `6b51e5e04` L hdr-and — feat(Android, Stack v5): handle header configuration and custom subviews ([#3796](https://github.com/software-mansion/react-native-screens/commit/6b51e5e04)) — new `ls-stack-header-config` + `ls-stack-header-subview` elements; Fabric state → `ShadowStateProxy`/`ShadowStateUpdating`; subview offsets stored but not applied in align (Toolbar positions views natively); `Providing.view` renamed to `subviewView` (clash with LynxUI.getView); background stretching needs `hasBackgroundSubview` + explicit `width/height: 100%` (absolute-fill does not resolve against custom-measure constraints); known issue: flex/absolute placement of content INSIDE subviews is ignored by the engine (sizes work, positions land top-left)
- [x] `c3077147b` L hdr-and — feat(Android, Stack v5): add back button to header with customization ([#3883](https://github.com/software-mansion/react-native-screens/commit/c3077147b)) — Fresco-based ImageLoader ported (Fresco is Lynx's standard image-service engine); `imageSource` icons take a plain URI string instead of RN assets; tint color as CSS string parsed natively
- [x] `6ae17e0b7` S hdr-and — docs(Android, Stack v5): add missing docs to StackHeaderConfig ([#3897](https://github.com/software-mansion/react-native-screens/commit/6ae17e0b7)) — docs adapted where the API diverges (uri-based imageSource, Lynx layout engine)
- [x] `12edf296f` S core-js — chore(types): remove nullability from some `children` props ([#3918](https://github.com/software-mansion/react-native-screens/commit/12edf296f)) — applied to StackHost children and header subview Component/children
- [x] `0d2d8a333` M hdr-and — feat(Android, Stack v5): add support for header scroll flags ([#3908](https://github.com/software-mansion/react-native-screens/commit/0d2d8a333)) — incl. StackHeaderAppBarLayoutBehavior overshoot fix and JS type-based defaults
- [x] `ddda1e0e0` S hdr-and — fix(Android, Stack v5): prevent crash when subview with (0, 0) size is added to small header ([#3927](https://github.com/software-mansion/react-native-screens/commit/ddda1e0e0)) — on Lynx the removed fallback reported spec-size instead of crashing; sizing is enforced from the engine now
- [x] `d3abbe712` S example — refactor(Example): TabsContainer and StackContainer refactor ([#3925](https://github.com/software-mansion/react-native-screens/commit/d3abbe712)) — StackContainer part only; components resolved via useComponentsByName instead of living in reducer state
- [x] `7c5a6bf0e` S hdr-and — fix(Android, Stack v5): change header subview type to React.ComponentType ([#4079](https://github.com/software-mansion/react-native-screens/commit/7c5a6bf0e)) — subview slots take `render: () => ReactElement` now
- [x] `88572ae6a` M hdr-and — feat(Android, Stack v5): ensure StackHeaderCoordinator teardown on Screen fragment destroyed ([#4096](https://github.com/software-mansion/react-native-screens/commit/88572ae6a))
- [x] `efc2b1fa8` L hdr-and — feat(Android, Stack v5): add toolbar menu items base implementation to header ([#3965](https://github.com/software-mansion/react-native-screens/commit/efc2b1fa8)) — Fabric view command → `@LynxUIMethod` + `NodesRef.invoke`; click event via LynxCustomEvent; verified on device
- [x] `713dcc6fc` M hdr-and — feat(Android, Stack v5): expose showAsAction prop for toolbar menu items ([#4101](https://github.com/software-mansion/react-native-screens/commit/713dcc6fc)) — verified on device (item promoted to toolbar button)
- [x] `b16e7ed3d` L hdr-ios — feat: Implement subviews layout in header for iOS stack v5 ([#3868](https://github.com/software-mansion/react-native-screens/commit/b16e7ed3d)) — new `ls-stack-header-item` + `ls-stack-header-item-spacer` elements; one runtime-branched JS config component (`SystemInfo.platform`) instead of RNS platform files; Fabric state → `RNSShadowStateProxy`/`RNSShadowStateUpdating` (no manual relayout kick needed on iOS - `setNeedsLayout` schedules via LynxUILayoutTick); config shadow node = `LynxCustomMeasureShadowNode` measuring children AT_MOST against the reported navbar frame; item offsets stored, not applied (UIKit positions the reparented views); config UI opts into `hasCustomLayout` + drops view-level child insertion; hit-testing probes reparented item views in window coords via shouldHitTest/containsPoint/hitTest overrides (analog of Android `findUIWithCustomLayout`; containsPoint is needed because the engine gates hitTest against the config's own bounds, which don't cover the navbar) — tap chain verified at runtime (screen hitTest resolves the pressable inside a navbar item); content is not inset below the navbar - faithful: NO commit in the current queue insets v5 iOS content or consumes `transparent` on iOS; RNS examples rely on UIKit auto-inset of native scroll views, which Lynx's scroller opts out of (contentInsetAdjustmentBehavior=Never) - our examples approximate it with a static paddingTop; verified on simulator: items+spacers, title/subtitle, largeTitle(+Enabled), largeSubtitle view, titleView/subtitleView, hidden, push with back button
- [x] `39ab47733` L hdr-ios — feat: Initial code for menu in header items ([#4138](https://github.com/software-mansion/react-native-screens/commit/39ab47733)) — `menu` prop (nested menu/menuItem tree) parsed by `RNSStackHeaderMenuMapper` straight from the Lynx NSDictionary (no folly conversion); menu opening not verified (needs a tap); example: TestStackHeaderMenuIOS
- [x] `17ec5db41` L hdr-and — feat(Android, Stack v5): toolbar menu item icon and icon tint color ([#4105](https://github.com/software-mansion/react-native-screens/commit/17ec5db41)) — `IconResolver` shared with back button; icons via uri strings (no resolveAssetSource) and tint colors as CSS strings parsed natively (no processColor); verified on emulator: imageSource icons, purple tint, command icon swap with tint read-back preserved
- [x] `19f550969` M hdr-and — feat(Android, Stack v5): add support for extended tinting and enforce M3 icon size for back button ([#4126](https://github.com/software-mansion/react-native-screens/commit/19f550969)) — `backButtonTintColor` → `...Normal` + Pressed/Focused (CSS strings); shared `getResizedDrawable` (with density fix) reused by toolbar menu; verified on emulator (custom icon resized to 24dp + purple tint)
- [x] `8c158ab95` L hdr-ios — feat: Handle onPress in iOS Menu ([#4148](https://github.com/software-mansion/react-native-screens/commit/8c158ab95)) — required `id` on menu elements; `onPress` resolved in JS by id via `OnMenuItemPress` LynxDetailEvent (instead of the codegen DirectEventHandler); the menu prop is serialized without the onPress callbacks (Lynx props must be serializable - RNS relies on the RN bridge dropping functions); chain verified on simulator by invoking didPressMenuItem (opening the UIMenu itself needs a manual tap)
- [x] `3d47a11dc` L hdr-and — refactor(Android, Stack v5): separate React and native domains in header implementation ([#4150](https://github.com/software-mansion/react-native-screens/commit/3d47a11dc)) — provider/observer/delegate/applicator architecture with bitmask invalidation flags; UIManagerListener transaction bracket → `onPropsUpdated` with an isInsidePropsUpdate guard; out-of-band flushes (child attach/detach, subview changes) deferred one frame (RNS's didMountItems runs after Fabric applied layout - flushing synchronously on Lynx attached 0x0 subviews to the Toolbar); teardown via LynxUI `destroy()`; StackHost UIManager plumbing n/a on Lynx; regression-tested on emulator (types, subviews at runtime, background+parallax, collapse, back button, toolbar menu icons+command)
- [x] `1e177250d` L hdr-ios — feat: Add toggling items with managed flow to menu ([#4194](https://github.com/software-mansion/react-native-screens/commit/1e177250d)) — ported 1:1 (toggle state tracker, singleSelection radio hierarchy, menu rebuild after selection change); adaptations: OnMenuSelectionChange as LynxDetailEvent payload (no codegen std::vector conversion), onSelectionChange stripped from the serialized menu prop alongside onPress, RCTAssert → NSAssert; verified on simulator via lldb-invoked UIAction handlers (toggle event, radio switch + deselect, no event on re-select, action press, tracker state across rebuilds)
- [x] `79bbe6d44` S core-and — fix(Android, Stack v5): set `needsCustomLayoutForChildren` to `true` for `StackHost` ([#4200](https://github.com/software-mansion/react-native-screens/commit/79bbe6d44)) — n/a on Lynx: `StackHostComponent.needCustomLayout() = true` (the Lynx analog of `needsCustomLayoutForChildren`) has been in place since the initial host port, because the fragment-managed hierarchy required opting out of engine child layout from day one; screens are sized by native layout pushed back through the shadow state proxy, so the stale-Yoga-layout failure mode this RNS fix addresses cannot occur
- [x] `69d05bd3d` M hdr-ios — feat: Rename label and key to title and id in Stack v5 header items ([#4208](https://github.com/software-mansion/react-native-screens/commit/69d05bd3d)) — ported 1:1 (item `label`→`title` incl. the native prop, item/spacer `key`→`id`); examples updated; verified on simulator (labeled item still renders)
- [x] `ee24415b6` L hdr-and — feat(Android, Stack v5): add support for nested menus in toolbar menu ([#4210](https://github.com/software-mansion/react-native-screens/commit/ee24415b6)) — ported 1:1 (`toolbarMenu` tree prop, per-item `onPress`, MenuMapper + ReadableMapExt extraction, immutable MenuConfig tree, internal visibility sweep, event rename Clicked→Press); adaptations: parseMenu takes a nullable ReadableMap (no codegen Dynamic/UnsafeMixed and no DO_NOT_USE workaround needed on Lynx), image icon uri arrives as a plain string (no resolveAssetSource map), CSS-string colors (no processColor), event via LynxCustomEvent `OnToolbarMenuItemPress`; RNS's Tabs-related changes in this commit are out of scope; verified on emulator (two-level nesting, deep item press, prop-driven rebuild, command on nested item, icon example regression)
- [x] `488954d9f` L hdr-ios — feat(Stack v5): Add onPress to Header Item on iOS ([#4217](https://github.com/software-mansion/react-native-screens/commit/488954d9f)) — ported 1:1 (itemId + respondsToOnPress props, primary UIAction bar button, MenuEventsDelegate → EventsDelegate rename with didPressHeaderItem, per-item event emitter); adaptations: item emitter is a lazy Lynx-style LynxDetailEvent wrapper (OnHeaderItemPress) instead of the eager codegen emitter; verified on simulator via lldb (primaryAction+menuOnLongPress on onPress items, menuOnTouchDown on menu-only items, press → 'onPress Item 0', menu action regression)
- [x] `8914da902` M core-and — feat(Android, Stack, Tabs, SVM): introduce `Container` & `ContainerItem` ([#4216](https://github.com/software-mansion/react-native-screens/commit/8914da902)) — stack part ported 1:1 (Container/ContainerItem protocol, ContainerItemSupport + ParentContainerItemRegistry, refersToCompat, StackContainer registration, determineTopFragment); adaptations: ContainerItem lives on StackScreenView (Lynx splits RNS's StackScreen into component+view; the protocol walks the native view hierarchy), ScrollViewSeeking conformance omitted (SVM epic not ported), ViewFinder ported partially (only the scroll-view lookup; Lynx AndroidScrollView extends NestedScrollView so the heuristic holds), Tabs changes out of scope; protocol is dormant until a consumer lands — smoke-tested on emulator
- [x] `fb70d7146` S core-ios — refactor(iOS, Stack): use RNSContainerHelpers for controller mounting ([#4231](https://github.com/software-mansion/react-native-screens/commit/fb70d7146)) — ported with the helper itself (RNS introduced it in the unported Tabs commit #4230): `ios/helpers/container/RNSContainerHelpers` added to the podspec; adaptations: reactSuperview/reactViewController walk → superview + responder-chain walk, initial-frame seeding kept from the old Lynx mounting path; the old superview guard in setupViewConstraints is superseded by the mountResult gate; smoke-tested on simulator
- [x] `c949a4460` L hdr-and — feat(Android, Stack v5): add support for groups in toolbar menu ([#4228](https://github.com/software-mansion/react-native-screens/commit/c949a4460)) — ported 1:1 (groups + itemType/initialToggleState/groupId, group metadata + checkability in applicator, selection handling + checked command in coordinator, JS assertions, OnToolbarMenuGroupSelectionChange event, toolbarMenuGroupDividerEnabled); adaptations: parseMenu returns Pair from a nullable ReadableMap, event via LynxCustomEvent; verified on emulator (checkbox multi-select, radio auto-deselect, submenu-scoped group, imperative checked, prop rebuild checkbox→radio)
- [x] `a66629829` M hdr-and — feat(Android, Stack v5): add support for disabling menu items ([#4236](https://github.com/software-mansion/react-native-screens/commit/a66629829)) — ported 1:1 (disabled prop + command option through config/options/mapper/applicator); new TestStackToolbarMenuDisabledAndroid example + disabled controls in the icon example; verified on emulator (disabled tint applied, disabled toolbar button does not fire onPress)
- [x] `ea2b889f1` M hdr-and — feat(Android, Stack v5): add toolbar menu item text related props ([#4234](https://github.com/software-mansion/react-native-screens/commit/ea2b889f1)) — ported 1:1 (titleCondensed + tooltipText, string props unified to nullable with Update-based command semantics, valueOrNull helper); new TestStackToolbarMenuTitleAndroid example; verified on emulator (long-press tooltip, full title in overflow, title command)
- [x] `86397af36` M hdr-and — feat(Android, Stack v5): add support for `menuTitle` (`headerTitle`) in submenus ([#4241](https://github.com/software-mansion/react-native-screens/commit/86397af36)) — ported 1:1 (menuTitle prop + command option, Item→Element rename across options/command/observer incl. the @LynxUIMethod name setToolbarMenuElementOptions); verified on emulator (submenu header from props and via command)
- [x] `c3031986a` S hdr-and — fix(Android): handle nullable Fresco bitmap ([#4242](https://github.com/software-mansion/react-native-screens/commit/c3031986a)) — applies (ImageLoader is ported); null-guard on underlyingBitmap added 1:1
- [x] `11ff7d58b` S hdr-and — chore(Android): make `ImageLoader` `onLoaded`'s result nullable ([#4247](https://github.com/software-mansion/react-native-screens/commit/11ff7d58b)) — applies (ImageLoader is ported); onLoaded fires with null on a null bitmap so icon-gated command updates are never dropped
- [x] `7e1ce750a` M core-ios — feat(iOS, Stack, Tabs): introduce `Container` & `ContainerItem` ([#4227](https://github.com/software-mansion/react-native-screens/commit/7e1ce750a)) — stack part ported 1:1 (RNSContainer/RNSContainerItem + support/registry holders, nav controller registers via didMoveToParentViewController, screen controller resolves cached→nested→heuristic); adaptations: RNSContentScrollViewProviding + RNSScrollViewFinder ported as dependencies from the unported SVM epic, no cached scroll view passed (ScrollViewSeeking/marker registration not ported), Tabs changes out of scope; verified via lldb (resolveCurrentContentScrollView returns the LynxScrollView through the heuristic)
- [x] `e38edd831` L hdr-ios — feat(iOS, Stack v5): Refactor Stack Header implementation and handle selective updates ([#4248](https://github.com/software-mansion/react-native-screens/commit/e38edd831)) — ported 1:1 (props read via RNSStackHeaderConfigDataProviding, RNSStackHeaderData + RNSStackNavigationItemCoordinator removed, header building merged into RNSStackScreenHeaderCoordinator with per-item RNSStackHeaderMenuTrackerRegistry, selective rebuildItemWithId + menu-only reapply driven by menuToggleCallback); adaptations: `children` protocol requirement satisfied by LynxComponent's inherited property (coordinator iterates children as `id`), RNSStackHeaderConfigView forwards didMoveToWindow in both directions so the component can detach from the coordinator, item's RNSViewFrameChangeDelegate conformance move n/a (Lynx item never implemented it - per-item shadow state not forwarded, see b16e7ed3d), RCTAssert/RNSLog → NSAssert/LLogInfo; verified on simulator via lldb (title change swaps only the target UIBarButtonItem while the second item and spacers keep identity, menu-only change applies the menu without an item rebuild, radio toggle reapplies the menu on the same bar button with tracker state preserved, onPress → JS "Pressed Item 2"); new example TestStackHeaderSelectiveUpdatesIOS
- [x] `7624a9c86` M hdr-ios — feat(Stack v5, iOS): Add `keepsMenuPresented` prop to menuItems ([#4261](https://github.com/software-mansion/react-native-screens/commit/7624a9c86)) — ported 1:1 (keepsMenuPresented on menu item data/mapper, allowed-key NSSets, decorateActionKeepsMenuPresented with iOS 16 gate, typing + menu example update); adaptations: RCTAssert → NSAssert, prop added to the Lynx menu-attr serialization (which strips callbacks explicitly); verified on simulator via lldb (all top-level UIActions of the example menu carry attributes=(KeepsMenuPresented) once the example flag is on, attributes=0 when off)
- [x] `b16d51623` S hdr-and — fix(Android): prevent Fresco recycled bitmap crash in ImageLoader ([#4274](https://github.com/software-mansion/react-native-screens/commit/b16d51623)) — applies (ImageLoader is ported); bitmap copied before wrapping in a drawable 1:1; RNS's Test4265 example NOT ported (it is Tabs-based - out of scope); regression-verified on emulator per the RNS test plan (imageSource toolbar icons still render in the icon example)
- [-] `501993080` S hdr-and — fix(Android, Tabs): handle platform color in tabs appearance props ([#4284](https://github.com/software-mansion/react-native-screens/commit/501993080)) — N/A: the fix makes `parseColor` resolve RN `PlatformColor` maps via React's `ColorPropConverter` (and drops the String branch, since RN pre-processes color props to ints); on Lynx there is no `PlatformColor` JS API and no `ColorPropConverter` in the bridge-compat layer - colors arrive as CSS strings and the String branch of `parseColor` is the documented divergence (see 17ec5db41/19f550969), so there is nothing to port
- [x] `fa1959d3c` M hdr-ios — feat(Stack v5, iOS): Implement hitSlop for custom header items ([#4290](https://github.com/software-mansion/react-native-screens/commit/fa1959d3c)) — adaptations: RNS routes UIKit hit-testing (host hitTest → screen's weak headerConfig → config hitTest over the items' react subviews, whose hitTest honors hitSlop); on Lynx the engine already routes nav-bar touches through the config's custom hit-test chain (see b16e7ed3d), so the whole host/screen part is unnecessary - the port extends `findItemChildAtPoint` to also probe the item's Lynx children with their hit-slop-aware `containsPoint` (hit-slop lives on LynxUI, not UIView); pressRetentionOffset has no Lynx counterpart (stays dropped from the example), hitSlop maps to Lynx's stable `hit-slop` attr, RNS's key-based remount hack unnecessary (the prop updates at runtime); verified on simulator via lldb (slop 0: point 15px under the item rejected; slop 30: accepted and hitTest resolves the pressable LynxUI, point beyond the slop still rejected)
- [x] `1f38fb6e7` L hdr-ios — feat(Stack v5, iOS): Support icon loading in header items and menu ([#4277](https://github.com/software-mansion/react-native-screens/commit/1f38fb6e7)) — ported 1:1 (icon prop on items + menu elements, IconData/IconMapper/IconResolver with sync sfSymbol/xcasset + async source loading, placeholder-until-loaded, per-icon cache, menuToggleCallback → menuInvalidatedCallback rename, RNSImageLoading protocol on the config component); adaptations: RCTImageLoader (plumbed via C++ shadow-node state) → shared LynxImageLoader wrapped in a Lynx-flavored RNSImageLoadingHelper (no state plumbing needed), image/template sources are flat `{ uri }` objects (no resolveAssetSource - PlatformIconIOS adapted like the Android uri divergence), placeholder falls back to 1x1 (uri sources carry no intrinsic size), RNSLog/RCTLogWarn → LLogWarn; example TestStackHeaderIconIOS (Toast → "Last event" text; custom-icon-fill imageset added to the app asset catalog; small template asset - unscaled sources render at raw size); verified on simulator: sfSymbol star renders, imageSource loads async through the dev server and lands on the bar button + all menu elements (single cached UIImage, menu reapplied via menuInvalidatedCallback), xcasset resolves from Assets.car, templateSource gets renderingMode=alwaysTemplate and renders tinted; note: dev-server asset URLs embed the LAN IP at server start - stale IP after network change breaks image loads (restart the dev server)
- [x] `e2793da15` M hdr-ios — feat(Stack v5, iOS): Add displayInline flag to menu ([#4300](https://github.com/software-mansion/react-native-screens/commit/e2793da15)) — ported 1:1 (displayInline on menu data/mapper/coordinator → UIMenuOptionsDisplayInline, typing, new TestStackHeaderMenuOptionsIOS example); verified on simulator via lldb (flags off: Sort By has options=(SingleSelection); flags on: Sort By = (Inline|SingleSelection), nested Rating = (Inline); sfSymbol menu icons render as a sync-42 regression check)
- [x] `f0a3a819f` M hdr-ios — feat(Stack v5, iOS): Add displayAsPalette flag for menu ([#4307](https://github.com/software-mansion/react-native-screens/commit/f0a3a819f)) — ported 1:1 (displayAsPalette on menu data/mapper/coordinator → UIMenuOptionsDisplayAsPalette gated on iOS/tvOS 17, typing, palette item added to the menu-options example); verified on simulator via lldb (Text Style submenu builds with options=(Inline|Palette) when both flags on)
- [x] `f7431f3ea` M hdr-and — feat(Android, Stack v5): add `accessibilityLabel` for toolbar menu items ([#4250](https://github.com/software-mansion/react-native-screens/commit/f7431f3ea)) — ported 1:1 (accessibilityLabel through config/defaults/options/mapper, applied via MenuItemCompat.setContentDescription with Update semantics - Reset restores Android's default; typing with RNS docs); new TestStackToolbarMenuA11yAndroid example (imageSource icon as uri string); verified on emulator via uiautomator (prop sets content-desc "Accessibility for Alpha" on the toolbar button, command updates it to "Updated label", command with undefined restores the default "Alpha")
- [-] `59dd58a84` S core-and — fix(Android, Stack v5): Prevent screen unmounting on fast navigation ([#4342](https://github.com/software-mansion/react-native-screens/commit/59dd58a84)) — N/A: despite the "Stack v5" title, the bug and the fix live in the v4 `ScreensCoordinatorLayout` (the commit touches only `rnscreens/stack/views/ScreensCoordinatorLayout.kt`); lynx-screens is v5-only, so there is nothing to port (decision confirmed 2026-08-20)
- [x] `37177c97b` L hdr-and — feat(Android, Stack v5): support sending multiple updates via view command ([#4243](https://github.com/software-mansion/react-native-screens/commit/37177c97b)) — ported 1:1 (updateToolbarMenuElements batch command with serial FIFO StackHeaderToolbarMenuUpdateQueue, atomic apply after all icons resolve, one coalesced group event per batch via apply/emit split in the coordinator layout, ImageLoader "always completes" contract + resolveImage helper, IconResolver → PropIconResolver and StackHeaderToolbarUpdate → StackHeaderToolbarFieldUpdate renames, command icons bypass the prop resolver/cache); adaptations: NodesRef.invoke passes one params map so the batch array travels under an "updates" key (RNS codegen takes the array directly), ArrayDeque.addLast → add (K1/stdlib resolution failure), Tabs changes out of scope; new TestStackToolbarMenuBatchCommandsAndroid example + all 8 command-using examples swept to the new API; verified on emulator (Select All → one coalesced fruits event with 4 ids, cross-group batch → only the changed group emits, ordering race → last event reflects the later command with apple absent despite the slow image, failing image resolves to cleared icon and the follow-up batch still applies)
- [x] `693082b36` L hdr-ios — feat(Stack v5, iOS): Implement basic view commands for iOS header menu ([#4339](https://github.com/software-mansion/react-native-screens/commit/693082b36)) — ported 1:1 (setMenuItemOptions/setMenuOptions commands changing title/icon/toggleState, RNSStackHeaderMenuFinder + RNSStackHeaderMenuUpdateOptions, immutable menu-tree replacement in the menu coordinator, item keeps prop copies via titleProp/iconProp/menuProp, tracker gains setToggleState/toggleStateEquals and drops toggleStateChanged - selection events now always emit and re-selecting a radio early-returns, coordinator setToggleState resolves the single-selection root and emits the selection event); adaptations: commands are LYNX_UI_METHODs taking {menuElementId, options} params (no codegen array wrapper - extractOptionsDict reads the options key), RCTAssert/RCTLogWarn → NSAssert/LLogWarn, no icon asset resolution step on the JS side (uri passthrough); menu example gains command pickers; verified on simulator via a temporary on-mount effect + lldb (action re-titled 'Changed' with star.fill, submenu re-titled 'New Title' with heart.fill, toggleState:true sets UIAction state=On and delivers Selected "toggle-1-2" to JS)
- [x] `9c62292d8` S core-and — chore(Android): bump Material to 1.14.0 ([#4348](https://github.com/software-mansion/react-native-screens/commit/9c62292d8)) — added to the queue 2026-08-20 (Material 1.14 is important in RNS and a prerequisite for the M3 Expressive App Bar theme); bumped 1.13.0 → 1.14.0 in android/build.gradle.kts; regression-checked on emulator per the RNS Stack v5 test plan (toolbar menu icons render, large collapsing header renders)
- [x] `96a4107b1` S hdr-and — feat(Android, Stack v5): use M3 Expressive theme for App Bar ([#4365](https://github.com/software-mansion/react-native-screens/commit/96a4107b1)) — ported 1:1 (wrappedContext theme → Theme_Material3Expressive_DayNight_NoActionBar); verified on emulator (large header renders in the Expressive style - larger title/height than plain M3)
- [ ] `d6e39013f` L core-and — refactor(Android, Stack v5): split large classes, reorganize package structure ([#4354](https://github.com/software-mansion/react-native-screens/commit/d6e39013f)) — align lynx-screens package layout with RNS
- [ ] `2828bb2a5` M hdr-and — feat(Android, Stack v5): expose `liftOnScroll` prop for `small` header, integrate with SVM ([#4368](https://github.com/software-mansion/react-native-screens/commit/2828bb2a5)) — depends on ScrollViewMarker, which lynx-screens doesn't have; decide scope on review
- [ ] `cff4f4131` L hdr-ios — feat(Stack v5, iOS): Add titleMenu ([#4349](https://github.com/software-mansion/react-native-screens/commit/cff4f4131))
- [ ] `406318890` S example — refactor(examples, stack v5): erase options as required from StackRouteConfig ([#4419](https://github.com/software-mansion/react-native-screens/commit/406318890))
- [ ] `aa9236bae` S hdr-and — fix(Android, Stack v5): change order of toolbar menu item prop application ([#4507](https://github.com/software-mansion/react-native-screens/commit/aa9236bae))

## Reviewed, likely not applicable (confirm during walk-through)

- [ ] `d4f3d71fc` — fix(ios): guard `+load` with `#ifdef RCT_DYNAMIC_FRAMEWORKS` (#3828) — RN build-system specific
- [ ] `d372cb8e6` — fix(iOS, Stack v5): framework-style imports for React core headers (#4402) — RN-specific includes
- [ ] `836c46c13` — clang-format bump (#3972); `baeac78c6` — hide react symbols from swift (#4224); `749df7a71` — RMTO to obj-c (#4420) — RNS toolchain/infra
- [ ] `7708a4af2` — react-native@0.87 support (#4375) — N/A for Lynx
- [ ] RNS repo restructuring: `f52cd54c4`, `a7e79aca8`, `7190dfe80`, `b608e9a28`, `b54af6d12`, `68dce5222`, `d8b3827b1`, `d8484a058` (gamma → top-level, v4 → legacy, `RNS_GAMMA_ENABLED` removal), `fdc2fba17` (out of experimental) — lynx-screens never had gamma dirs; only naming alignment may be worth mirroring

## Out of scope

- **FormSheet v5** — standalone component in RNS, ~45 commits in range (basic setup, detents, lifecycle events, preventNativeDismiss, appearance). Decided out of scope for this sync (2026-08-18). If that changes, the queue can be regenerated the same way as this one.
- **Tabs, Split, ScrollViewMarker, SafeArea, ScrollToTopGuard** — components lynx-screens doesn't port.
- **Stack v4 / legacy** — lynx-screens is v5-only.

Note: `2828bb2a5` (liftOnScroll) integrates with ScrollViewMarker, which lynx-screens lacks — when its turn comes, port without the SVM integration (or record `[-]` with rationale).

## Stack v5 e2e tests in range (no e2e infra in lynx-screens — use as manual test scenarios)

`b6ebfff6d` (simple navigation), `f325544fb`/`edaabbb61`/`38f1ad13c` (preventNativeDismiss), `9af76dd0a` (lifecycle events), `7bfc30bdc`/`fc6d911af`/`c39df3bc4`/`60009aff5`/`4174a5a3e`/`34bed0cbd` (toolbar/menu), `d45d50a0b`/`fb6cf3940`/`cec9234dd`/`a608b9b51` (header subviews/updates/icons), `c03058060` (lift-on-scroll)
